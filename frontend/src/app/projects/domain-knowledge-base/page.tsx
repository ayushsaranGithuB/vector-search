import { SearchLayout } from "@/components/search-layout";

export default function DomainKnowledgeBasePage() {
  return (
    <SearchLayout
      projectName="Domain Knowledge Base"
      projectDescription="A domain-specific knowledge project demonstrating the platform's reusability across different contexts."
    />
  );
}